package main

// SyncCmd represents the `sync` command.
type SyncCmd struct{}

// Run the Sync command.
func (*SyncCmd) Run() error {
	// get keycloak URL and credentials from the environment
	// init client
	// run sync
	return nil
}
